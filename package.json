{
  "name": "sls-use-vue-ui",
  "version": "0.0.0",
  "description": "使用测试的vueui",
  "private": true,
  "keywords": [],
  "scripts": {
    "dev": "cooking watch -p",
    "dist": "cooking build -p",
    "deploy": "cooking build --no-color",
    "test": "cooking test karma start --single-run"
  },
  "license": "ISC",
  "dependencies": {
    "vue": "^2.0.1",
    "babel-polyfill": "*"
  },
  "devDependencies": {
    "cooking-vue2": "*",
    "vue-template-compiler": "^2.5.9"
  }
}
